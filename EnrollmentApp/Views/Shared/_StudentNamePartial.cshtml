@using EnrollmentApp.Controllers;
@{

    StudentController s = new StudentController();
    string username = User.Identity.Name;
    Student student = s.Get(null, username);

    string fullname = student.FullName;
}

@fullname
